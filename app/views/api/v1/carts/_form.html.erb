<%= form_with(model: api_v1_cart) do |form| %>
  <% if api_v1_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v1_cart.errors.count, "error") %> prohibited this api_v1_cart from being saved:</h2>

      <ul>
        <% api_v1_cart.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
